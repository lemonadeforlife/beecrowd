#include<stdio.h>

int main(){
    int n;
    while (scanf("%d", &n) != EOF)
    {
        if (n%2==0){
            continue;
        }
        int start = (n+1)/2;
        for (int i = start; i <= n; i++){
            
        }
    }
    
}